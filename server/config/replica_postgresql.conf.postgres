# Network
listen_addresses = '*'
port = 5432

# Replication
wal_level = replica
max_wal_senders = 10
wal_keep_size = 1GB
max_replication_slots = 10

# Standby
hot_standby = on

# This server is a replica
primary_conninfo = 'host=postgres_primary port=5432 user=replicator password=ThZ3d1112'
primary_slot_name = 'replica_slot'